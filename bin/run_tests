#!/usr/bin/env bash

set -eu

# Pass "behat" as an extra command-line argument if you prefer using Behat over PHPUnit for use case tests and
# end-to-end tests

docker-compose run --rm php sh ./run_tests.sh "$@"
